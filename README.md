# shorturls

## Install

1. Install Node.js

    via Homebrew:
    ```
    brew install node
    ```
    or from [nodejs.org](https://nodejs.org/en/)

1. Install dependencies

    ```
    npm install
    ```

## Run

1. Start the server

    ```
    npm start
    ```
